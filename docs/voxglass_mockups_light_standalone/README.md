# Voxglass Light Standalone Mockups

One self-contained HTML file per screen. CSS is embedded inside each file. No external dependencies.

Corrected against the PNG board: 11-screen set, Home/Browse naming, mini-player placement, bottom tab structure, source/settings screens, and light/dark visual variants.
